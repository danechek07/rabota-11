# Домашнее задание к лабораторной работе №11
(Вариант 19)

## Условие задачи
С одномерным массивом, состоящим из `n` вводимых с клавиатуры положительных и отрицательных целых чисел, выполнить следующие вычисления:  
**19. Среднее значение положительных элементов массива, расположенных по нечётным индексам.**  
(Использовать условный оператор `if` не рекомендуется)

## 1. Алгоритм и блок-схема

### Алгоритм
1. **Начало**  
2. Объявить переменные:  
   - `n` — размер массива,  
   - `arr[n]` — массив целых чисел,  
   - `sum` — сумма положительных элементов на нечётных индексах,  
   - `count` — количество таких элементов.  
3. Запросить у пользователя ввод `n`.  
4. Запросить ввод `n` элементов массива.  
5. Инициализировать `sum = 0`, `count = 0`.  
6. Пройти по массиву с индексами от `1` до `n-1` (нечётные индексы):  
   - Если элемент положительный — прибавить к `sum`, увеличить `count`.  
7. Вычислить среднее: `avg = sum / count` (если `count > 0`).  
8. Вывести результат.  
9. **Конец**

### Блок-схема
<img width="523" height="1082" alt="11" src="https://github.com/user-attachments/assets/677d4185-737d-4b41-a573-705c30c370dc" />

[chema_11.drawio
](https://github.com/danechek07/rabota-11/blob/main/11.png)
## 2. [Реализация программы](https://github.com/danechek07/rabota-11/blob/main/dz11.c)

## 3. Результаты работы программы

- **Ввод:**
  ```
  Введите размер массива n: 7
  Введите 7 элементов:
  -3 4 -1 2 5 -6 8
  ```
  **Вывод:**
  ```
  Среднее значение положительных элементов на нечётных индексах: 3.000000
  ```

- **Ввод:**
  ```
  Введите размер массива n: 5
  Введите 5 элементов:
  -2 4 3 6 -1
  ```
  **Вывод:**
  ```
  Среднее значение положительных элементов на нечётных индексах: 5.000000
  ```

- **Ввод:**
  ```
  Введите размер массива n: 4
  Введите 4 элементов:
  -1 -2 -3 -4
  ```
  **Вывод:**
  ```
  Нет положительных элементов на нечётных индексах.
  
 
## 4. Информация о разработчике
Петрукович Данила, бИЦ-251
